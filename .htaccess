RewriteEngine On

# Redirect www to non-www:
RewriteCond %{HTTP_HOST} ^www.ithacash.com [NC]
RewriteRule ^(.*)$ http://ithacash.com/$1 [L,R=301]

# Redirect login to Cyclos
Redirect /login https://communities.cyclos.org/Ithacash