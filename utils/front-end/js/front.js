$(function() {

    // $(window).scroll(function() {

    // 	map_offset_top = $('#invigorating-our-economy').offset().top;
    // 	map_height = $('#invigorating-our-economy').height();
    // 	map_offset_bottom = map_offset_top + map_height
    // 	intro_offset = $('#intro').offset().top;
    // 	window_scroll = $(window).scrollTop();

    // 	if ( window_scroll < map_offset_bottom && window_scroll > intro_offset) {
    // 		background_offset = (window_scroll / 5) - 200;
    // 		$('#invigorating-our-economy').css('background-position', '0px ' + background_offset + 'px');
    // 	}

    // })

    // $('#panels').slick({
    // 	  infinite: true,
    // 	  autoplay: true,
    // 	  autoplaySpeed: 7000,
    // 	  prevArrow: $('#left-arrow button'),
    // 	  nextArrow: $('#right-arrow button')
    // }).on('afterChange', function() {
    // 	var currentSlide = $(this).slick('slickCurrentSlide');
    // 	if ( currentSlide === 0 ) { $(this).slick('slickPlay'); }
    // });
})
